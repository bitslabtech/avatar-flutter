# macOS
.DS_Store
._.DS_Store
._*

# VSCode workspace duplicates (keep only avatar.code-workspace)
avatar2.code-workspace
avatar3.code-workspace
avatar4.code-workspace
avatar5.code-workspace
avatar6.code-workspace
avatar7.code-workspace

# Misc OS
Thumbs.db
desktop.ini

# Local working folders (design assets, product lists, AI tool artifacts)
Product list/
antigravity work/
assets/

# Database dumps & restore scripts (sensitive — transfer manually, never commit)
*.sql
*_backup_*.sql
restore_db.ps1
